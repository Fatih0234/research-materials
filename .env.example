# =============================================================================
# TrustBench Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values
# Never commit .env to version control!

# =============================================================================
# OpenRouter Configuration (Iteration C2 - Agent-Trust Integration)
# =============================================================================

# Required: OpenRouter API Key
# Obtain from: https://openrouter.ai/keys
# Format: sk-or-v1-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
OPENROUTER_API_KEY=your-openrouter-key-here

# Optional: OpenRouter API Base URL
# Default: https://openrouter.ai/api/v1
# Override only for testing/staging endpoints
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# Optional: Attribution (recommended - appears in OpenRouter dashboard)
# Your application's URL (HTTP-Referer header)
OPENROUTER_APP_URL=https://github.com/your-username/llm-applied-economics

# Your application's name (X-Title header)
OPENROUTER_APP_NAME=TrustBench Research

# =============================================================================
# Model Selection (for future use - currently hardcoded in agent-trust)
# =============================================================================

# Default model for experiments (OpenRouter format)
# Examples: gpt-3.5-turbo, gpt-4, openai/gpt-4o
TRUSTBENCH_DEFAULT_MODEL=gpt-3.5-turbo

# Fallback models (comma-separated, for future routing feature)
# Examples: openai/gpt-3.5-turbo,anthropic/claude-3.5-sonnet
TRUSTBENCH_FALLBACK_MODELS=

# =============================================================================
# Backward Compatibility (Optional)
# =============================================================================

# Legacy OpenAI API key (fallback if OPENROUTER_API_KEY not set)
# Only needed if you want to use OpenAI directly without OpenRouter
# OPENAI_API_KEY=your-openai-key-here

# =============================================================================
# Usage Instructions
# =============================================================================
#
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Edit .env and add your OPENROUTER_API_KEY
#
# 3. Verify .env is gitignored (should already be in .gitignore)
#
# 4. Load environment variables:
#    # Method 1: Source in shell (bash/zsh)
#    set -a && source .env && set +a
#
#    # Method 2: Python script (automatic with python-dotenv)
#    from dotenv import load_dotenv
#    load_dotenv()
#
# 5. Run smoke test:
#    python tools/test_openrouter_agent_trust.py
#
# 6. Run agent-trust demo:
#    cd vendor/agent-trust
#    python agent_trust/no_repeated_demo.py
#
# For more details, see:
# - specs/03_openrouter_env.md (environment variable reference)
# - vendor/agent-trust/README_OPENROUTER.md (OpenRouter setup guide)
# - plans/04_agent_trust_integration.md (integration design)
